This App i a basic example of the layered architecture and the CQRS Pattern design.

To install: run `composer install` and then run the migrations to create your database with `php bin/Migrate.php`.

take a look into the routes file:
- goto /register to register a user.
- goto /login to log in
- goto /submit to submit an entry